from setuptools import setup, find_packages

setup(
    name='langchain',
    version='0.1',
    packages= packages=find_packages(include=['libs']),
    install_requires=[
        # Add your dependencies here
    ],
)