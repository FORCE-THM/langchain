from setuptools import setup, find_packages

setup(
    name='langchain-community',
    version='0.2.15',
    packages=find_packages(include=['langchain_community']),
    install_requires=[
        # Add your dependencies here
    ],
)