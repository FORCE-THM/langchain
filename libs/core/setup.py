from setuptools import setup, find_packages

setup(
    name='langchain-core',
    version='0.2.38',
    packages=find_packages(include=['langchain_core']),
    install_requires=[
        # Add your dependencies here
    ],
)