from setuptools import setup, find_packages

setup(
    name='langchain-text-splitters ',
    version='0.2.2',
    packages=find_packages(include=['langchain_text_splitters']),
    install_requires=[
        # Add your dependencies here
    ],
)